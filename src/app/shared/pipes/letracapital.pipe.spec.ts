import { LetracapitalPipe } from './letracapital.pipe';

describe('LetracapitalPipe', () => {
  it('create an instance', () => {
    const pipe = new LetracapitalPipe();
    expect(pipe).toBeTruthy();
  });
});
